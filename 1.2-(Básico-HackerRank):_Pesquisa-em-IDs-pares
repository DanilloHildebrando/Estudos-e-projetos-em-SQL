// Retorna as cidades em que o último caráctere do ID da estação é uma das seguintes opções: '0', '2', '4', '6' ou '8'.

select distinct city from station where RIGHT(ID, 1) IN ('0', '2', '4', '6', '8');
